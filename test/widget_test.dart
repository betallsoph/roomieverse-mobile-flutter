import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts', (WidgetTester tester) async {
    // Placeholder test — Firebase init requires native plugins
    expect(true, isTrue);
  });
}
